node_modules/
coverage/
dist/
.tmp/
*.tsbuildinfo

# feng's own runtime workspace (created when feng is run inside this repo, e.g.
# absorbing system-layer feedback). It is live runtime state, not source.
.feng/

# Third-party agent clones kept locally for reference (not part of feng)
AssistantAgent/
CodeWhale/
Shinsekai/
hermes-agent/
learn-claude-code/
opencode/

# Editor / tooling scratch
.agents/
.claude/
.idea/
